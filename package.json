{
  "name": "inventorysync",
  "version": "1.0.0",
  "private": true,
  "description": "ERP integration service — linting for the static admin UI",
  "scripts": {
    "lint": "eslint src/InventorySync.Api/wwwroot/js"
  },
  "devDependencies": {
    "eslint": "^10.11.0"
  }
}
